import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  contain: {
    display: "flex",
    flexDirection: "row",
    marginTop: 16,
    borderRadius: 6,
    backgroundColor: "#242424",
    width: "100%",
    padding: 10,
  },
});
