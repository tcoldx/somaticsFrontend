import { StyleSheet } from "react-native";

export const footer = StyleSheet.create({
  footContainer: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    height: 100,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    marginBottom: 8,
    alignItems: "center",
    justifyContent: "space-around",
  },
  linearGradient: {
    position: "absolute",
    left: 0,
    right: 0,
    bottom: 0,
    height: 100,
  },
});
